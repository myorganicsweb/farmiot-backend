// ==========================================
// FARMIOT ESP32-C3: UNIVERSAL OTA CLIENT
// ==========================================

#include <WiFi.h>
#include <WiFiClientSecure.h>
#include <WebSocketsClient.h> // Install "WebSockets" by Markus Sattler
#include <ArduinoOTA.h>
#include <ESP32httpUpdate.h>  

// --- WIFI CREDENTIALS ---
const char* ssid = "scoobydoo gateway";
const char* pwd  = "scoobydoo";

// --- YOUR RENDER BACKEND URL ---
const char* wsServer = "wss://farmiot-backend.onrender.com"; 
const char* deviceId = "esp32_01";

// --- ESP32-C3 PIN ---
int ledPin = 8;

WebSocketsClient webSocket;

void setup() {
  // 1. SET UP LED
  ledcAttach(ledPin, 5000, 8);
  ledcWrite(ledPin, 0); 

  // 2. CONNECT TO WIFI
  WiFi.mode(WIFI_STA);
  WiFi.begin(ssid, pwd);
  
  int attempts = 0;
  while (WiFi.status() != WL_CONNECTED) {
    delay(500);
    attempts++;
    // Blink while connecting
    ledcWrite(ledPin, 255);
    delay(100);
    ledcWrite(ledPin, 0);
    delay(400);
    
    if (attempts > 40) {
      // Fast blink error if Wi-Fi fails
      while(1) { ledcWrite(ledPin, 255); delay(100); ledcWrite(ledPin, 0); delay(100); }
    }
  }
  
  // 3. CONNECT TO REMOTE OTA SERVER (Universal - no port number)
  // This automatically uses port 443 via wss://
  webSocket.begin(wsServer, "/" + String(deviceId));
  webSocket.onEvent(webSocketEvent);
  webSocket.enableHeartbeat(30000, 5000, 2);

  // 4. LED SOLID BLUE = READY
  ledcWrite(ledPin, 255); 
}

void loop() {
  webSocket.loop();
  delay(10);
}

// --- WEB SOCKET EVENT HANDLER ---
void webSocketEvent(WStype_t type, uint8_t * payload, size_t length) {
  switch(type) {
    case WStype_DISCONNECTED:
      ledcWrite(ledPin, 0);
      break;
    case WStype_CONNECTED:
      ledcWrite(ledPin, 255);
      break;
    case WStype_TEXT:
      String command = String((char*)payload);
      if (command.indexOf("ota_update") > 0) {
        performOTAUpdate(command);
      }
      break;
  }
}

// --- PERFORM OTA UPDATE OVER INTERNET ---
void performOTAUpdate(String command) {
  int start = command.indexOf("url\":\"") + 6;
  int end = command.indexOf("\"", start);
  String firmwareUrl = command.substring(start, end);
  
  // 3 quick flashes = Starting OTA update
  for(int i=0; i<3; i++) { ledcWrite(ledPin, 255); delay(100); ledcWrite(ledPin, 0); delay(100); }

  WiFiClientSecure client;
  client.setInsecure(); 

  t_httpUpdate_return ret = ESPhttpUpdate.update(client, firmwareUrl);
  
  switch(ret) {
    case HTTP_UPDATE_FAILED:
      while(1) { ledcWrite(ledPin, 255); delay(50); ledcWrite(ledPin, 0); delay(50); }
      break;
    case HTTP_UPDATE_OK:
      ledcWrite(ledPin, 255); delay(1000);
      ESP.restart();
      break;
  }
}